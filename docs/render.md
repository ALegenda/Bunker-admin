# Render: минимальная конфигурация

Рабочий адрес: https://bunker-vdk.ru. Окружение Render создано в Singapore;
`render.yaml` описывает его конфигурацию. Ресурсы созданы через API, без привязки
к Blueprint: повторное создание Blueprint может создать платные дубликаты.

**Перенос завершён 18 сентября 2026, около 07:52 по Владивостоку.**
Сайт, API, PostgreSQL, файловое хранилище и PDF работают на Render.
Восстановлен окончательный снимок после остановки записей на VPS:
329 карточек, 3 пользователя, 1 выпуск, 319 записей assets, 3 задания PDF;
проверены все 328 файлов хранилища. Старые сессии отозваны.

DNS: `A @ → 216.24.57.1`, `CNAME www → bunker-studio.onrender.com.`, TTL — час.
HTTPS, страницы, каталог, изображения, скачивание PDF, отказ гостю в редакторском
API и адрес обратного вызова Telegram проверены непосредственно на Render.
Полный вход через Telegram с подтверждением пользователя отдельно не выполнялся.
Плановый PDF-процесс успешно запустился после переноса.
Для сайта и PDF включено `After CI Checks Pass`, рабочая ветка — `master`.

Старый Nginx передаёт запросы клиентов с прежним DNS на Render, проверяя TLS.
Его временный upstream — `216.24.57.15` с SNI `bunker-vdk.ru`; этот адрес
используется только на время истечения старых DNS-кэшей. Старые контейнеры Bunker
и таймер их резервного копирования остановлены, данные и тома сохранены.
Временный SSH-ключ удалён; окончательный перенос выполнен одноразовой задачей
через зашифрованный AES-256-GCM файл. Файл передачи и временные секреты удалены.

## Ресурсы и стоимость

Конфигурация для небольшого сообщества (до 100 пользователей), без горизонтального
масштабирования и без изменения архитектуры приложения:

| Ресурс | Конфигурация | В месяц |
| --- | --- | --- |
| Сайт и API | 0,5 CPU, 512 МБ | $7 |
| PDF по расписанию | 1 CPU, 2 ГБ только на время выполнения | от $1 |
| Приватный MinIO | 0,5 CPU, 512 МБ | $7 |
| Диск файлов | 5 ГБ | $1,25 |
| PostgreSQL 17 | 256 МБ, диск 1 ГБ | $6 |
| **Базовая сумма** | | **от $22,25** |

Источник: [Render Pricing](https://render.com/pricing), проверено 18 сентября 2026.
Цена не включает налоги, возможную плату существующего workspace и превышение
лимитов трафика/сборок. На Hobby включены 5 ГБ исходящего трафика и 500 минут сборки;
сверх лимитов действует тарификация Render. Перед созданием проверить тариф и
оставшуюся квоту сервисов в аккаунте. Бесплатная PostgreSQL не предназначена для
постоянного хранения production-данных.

PDF-процесс запускается раз в пять минут, обрабатывает очередь и завершается. Поэтому
к ожиданию сборки добавляются до пяти минут и время запуска контейнера. Render не
запускает одновременно два экземпляра одного cron job. Его оплата — по времени
работы (около $0,00058/мин для 2 ГБ), минимум $1/месяц. Итоговая сумма может быть
выше базовой в зависимости от длительности запусков. Постоянный PDF worker на
таком же тарифе стоил бы $25/месяц. Пробная сборка полного документа при лимите
512 МБ без swap завершилась OOM, поэтому этот лимит для PDF не используется.

В текущем Hobby workspace уже исчерпаны включённые домены: основной адрес и `www`
добавляют $0,50/месяц. Пустой запуск cron при проверке занял около 22 секунд;
при таком времени и интервале пять минут cron стоит около $1,84 за 30 дней.
Итого ориентир — **$23,59/месяц**, округлённо $24, при небольшой нагрузке.
Это оценка, а не жёсткий лимит счёта: трафик, сборки, налоги и длительные PDF
могут увеличить сумму. Стоимость других приложений в workspace сюда не входит.

Локальная проверка Docker с 1 CPU / 2 ГБ прошла: PDF на 93 страницы (12,3 МБ),
полный цикл проверки 6,2 секунды, пик памяти около 1120 МиБ. Проверены запись в S3,
healthcheck, главная и отказ гостю в доступе к редакторскому API. Это измерение
на тестовой копии исходных данных, а не нагрузочный тест Render или гарантия
времени для будущих документов.

Все компоненты находятся в Singapore. База и MinIO доступны по приватной сети;
доступ PostgreSQL извне запрещён (`ipAllowList: []`). MinIO использует постоянный
диск и отдельного пользователя приложения, которому разрешены только чтение и
запись в bucket `bunker`. Административные реквизиты не передаются приложению.

## Запуск

1. Войти через `render login`. Проверить `render blueprints validate render.yaml
   --output json`: у результата должно быть `valid: true` (одного exit code мало).
2. Для нового окружения создавать ресурсы после согласования расходов.
   Рабочая ветка — `master`; сайт и PDF обновляются после успешного CI.
   Хранилище обновляется вручную, чтобы обычные изменения приложения не
   перезапускали MinIO с диском.
3. Задать `PUBLIC_ORIGIN` равным фактическому HTTPS-адресу web-сервиса; перенести
   `TELEGRAM_CLIENT_ID`, `TELEGRAM_CLIENT_SECRET`, `TELEGRAM_ADMIN_IDS` из текущего
   серверного окружения через секреты Render. Не копировать `.env` в репозиторий.
4. MinIO при запуске создаёт приватный bucket и ограниченного пользователя.
   Pre-deploy API выполняет миграции и идемпотентный seed. Worker запускается
   по расписанию. Для контейнера Chromium включён
   `CHROMIUM_NO_SANDBOX=true`, как и в текущем Docker-развёртывании.
5. Проверить `/api/health`, лендинг, каталог, изображения, защищённые маршруты и
   генерацию полного PDF. Для проверки входа на временном домене добавить точные
   origin и `/auth/callback` в Allowed URLs Web Login бота.

`deploy/render/start.sh` формирует S3 URL из hostname приватного сервиса и
запускает с `exec` нужный процесс. `backend/worker-once.ts` использует существующие
блокировки и аренду заданий, закрывает соединения и выходит при пустой очереди.
При временной ошибке подключения выполняются не более трёх попыток за запуск;
после постоянной ошибки cron завершается неуспешно, чтобы она была видна в Render.
Увеличение числа карточек или размеров изображений требует повторной проверки
лимита памяти PDF.

## Перенос действующего сайта

Seed содержит исходные карточки, но не пользовательские правки, профили, историю
и выпуски. Для переноса необходимо восстановить актуальный дамп PostgreSQL и
все объекты приватного bucket в **новые** ресурсы. Перед финальным дампом временно
остановить изменения данных на старом сайте, чтобы не потерять записи между
копированием и переключением. Проверить контрольные суммы файлов и количество
карточек/пользователей/выпусков. Восстановленные сессии отозвать.

После проверки копии подключить `bunker-vdk.ru` в Render, заменить `PUBLIC_ORIGIN`
и DNS, проверить HTTPS, Telegram-вход, сохранение карточки и PDF. Не подключать
`www` без продуманного редиректа: приложение принимает только Host основного
origin. Render healthcheck использует один из подключённых custom domains.
Старый VPS оставить доступным для отката. Откат после новых записей на Render
требует обратной синхронизации данных, одного возврата DNS недостаточно.

Перед сменой DNS заранее уменьшить TTL; REG.RU допускает минимум один час.
Старые записи могут оставаться в кэшах до истечения прежнего TTL. На время
перехода старый Nginx должен передавать запросы `/.well-known/acme-challenge/`
в Render с исходным Host, но без входящих служебных заголовков CDN
(`proxy_pass_request_headers off`). Это позволяет центру сертификации проверять
домен даже через старый адрес. После выпуска сертификата старый Nginx может
передавать обычные запросы на Render с SNI основного домена и обязательной
проверкой TLS-сертификата. Старые API и worker при этом должны быть остановлены.

## Обслуживание

- [Сайт](https://dashboard.render.com/web/srv-dalvne0ae00c73cphupg).
- [PDF](https://dashboard.render.com/cron/crn-dalvnlfcgkoc73fpdeig).
- [Хранилище](https://dashboard.render.com/pserv/srv-dalvn62d0e5s738pmbd0).
- [PostgreSQL](https://dashboard.render.com/d/dpg-dalvn5ncgkoc73fpbda0-a).

Старый VPS-деплой отключён переменной GitHub `DEPLOY_ENABLED=false`.
Telegram на Render получает ключи напрямую; обход через старый VPS не нужен.
При переносе отозваны старые сессии, потребуется повторный вход через Telegram.

Дамп перед переключением и все файлы сохранены на старом VPS в
`/opt/bunker/backups/render-migration-20260918` и в локальном игнорируемом `tmp/`.
Старую базу и MinIO не удалять до проверки работы. После новых записей на Render
для отката нужна обратная синхронизация, а не только смена DNS.

Для платной PostgreSQL доступен [PITR](https://render.com/docs/postgresql-backups):
на Hobby окно восстановления составляет до трёх дней с момента создания базы.
Render также автоматически создаёт [снимок диска](https://render.com/docs/disks#disk-snapshots)
каждые 24 часа и хранит снимки не менее семи дней.
Внешнее автоматическое резервное копирование объектов пока не настроено.
PITR и snapshot диска Render не заменяют проверенную согласованную
резервную копию базы вместе с файлами. Локальная команда `npm run backup`
использует Docker Compose и не является резервным копированием Render.

Документация платформы: [Docker](https://render.com/docs/docker),
[workers](https://render.com/docs/background-workers),
[cron jobs](https://render.com/docs/cronjobs),
[Blueprint](https://render.com/docs/blueprint-spec),
[healthchecks](https://render.com/docs/health-checks).
