# Registered domain; its A record must point to the deployment server.
# This example is for Caddy running on the same host as Docker.
bunker-vdk.ru {
    encode zstd gzip
    reverse_proxy 127.0.0.1:4173
}
