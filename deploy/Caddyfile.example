# Replace with a hostname you control, including a free DNS hostname.
# This example is for Caddy running on the same host as Docker.
bunker.example.org {
    encode zstd gzip
    reverse_proxy 127.0.0.1:4173
}
